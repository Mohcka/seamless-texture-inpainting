services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - HF_HOME=/app/.cache/huggingface/hub
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]